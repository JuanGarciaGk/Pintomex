(subir cambios)
git add .
git commit -m "Comentario"
git push

(crear una rama nueva)
git branch
git fetch origin
git branch -r
git checkout -b fernando origin/fernando
git switch master
hacer un merge entre rama master y rama fernando

(actualizar trabajo en ramas)
git fetch origin
git branch -r   (para ver las ramas exitentes)
git switch fernando  (para acceder a la rama)
git ranch  (para ve en qu erama estas ubicado)
git marge origin master   (para descargar los cambios de la rama actualizada)
para salir se utiliza ":qw"

git status  (para ver si esta actulizada la rama)

DROP DATABASE pintumex_pos;